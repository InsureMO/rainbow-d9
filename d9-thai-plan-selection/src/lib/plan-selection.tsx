import React from 'react';
import {PlanSelectionProps} from './types';
import {APlanSelection} from './widgets';

export const PlanSelection = (props: PlanSelectionProps) => {
	return <APlanSelection/>;
};
