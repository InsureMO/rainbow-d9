Specify whether the current configuration is effective.

> Note that configurations that are not effective will not be loaded when the application starts, so the effective status cannot be switched
> at runtime.